Help [=[

rng.rnbinom(n, p [, dest])
--------------------------

Returns random deviates from the negative binomial distribution with size `n`
(`n >= 0`) and probability `p` (`0 <= p <= 1`).

__See also__: `stat.dnbinom`
]=]
